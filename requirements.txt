# Основные зависимости с фиксированной мажорной версией.
# Используем корректный синтаксис `~=` и фиксируем основную версию,
# чтобы предотвратить дрейф окружения при установке.
pandas~=2.2
pyarrow~=15.0
loguru~=0.7
# Опционально для расширенной валидации (необязательно для запуска CLI):
# rdkit~=2023.3
